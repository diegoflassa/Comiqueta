---
description: Run the repository log and code hygiene sweep.
---

# Clean

Follow [conductor/workflows/clean.md](../../conductor/workflows/clean.md).

**Protected** - every filter catalogued in the log-filter KI survives the sweep, and `i` / `w` / `e` level
logs are never touched regardless of filter.
