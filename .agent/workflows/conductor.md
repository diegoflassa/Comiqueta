---
description: Abre o índice principal do Conductor
---

// turbo-all
Load project technical mapping.

1. `view_file` on `conductor/INDEX.md`.
2. `list_dir` on `conductor/` to see all available mapping docs.
