# Feature: Viewer

## Purpose
The `feature-viewer` module is the core consumption experience. It handles the display of content (likely comics or images, given the app name "Comiqueta").

## Architecture
*   **Complex UI**: likely contains custom views or heavy Compose layouts for zooming/panning.
*   **Performance**: Optimized for image loading and memory management.

## Public API
*   **Entry Point**: `ViewerRoute`, expecting arguments (e.g., `comicId`).
