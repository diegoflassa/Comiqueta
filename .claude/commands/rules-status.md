---
description: "Check this repository rules for internal contradictions, stale references and drift."
disable-model-invocation: true
---

This is a pointer. The workflow is [`.agents/workflows/rules-status.md`](../../.agents/workflows/rules-status.md): read that file now and follow it exactly - its relative links resolve from its own folder. Any arguments typed after the command are its input.
