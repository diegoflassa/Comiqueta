---
description: "Decide which tests to run for the current change and run them."
disable-model-invocation: true
---

This is a pointer. The workflow is [`.agents/workflows/test-scope.md`](../../.agents/workflows/test-scope.md): read that file now and follow it exactly - its relative links resolve from its own folder. Any arguments typed after the command are its input.
