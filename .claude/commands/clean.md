---
description: "Run the repository log and code hygiene sweep."
disable-model-invocation: true
---

This is a pointer. The workflow is [`.agents/workflows/clean.md`](../../.agents/workflows/clean.md): read that file now and follow it exactly - its relative links resolve from its own folder. Any arguments typed after the command are its input.
