---
description: "Remove a named log filter from the codebase and the catalogue."
disable-model-invocation: true
---

This is a pointer. The workflow is [`.agents/workflows/remove-filter.md`](../../.agents/workflows/remove-filter.md): read that file now and follow it exactly - its relative links resolve from its own folder. Any arguments typed after the command are its input.
