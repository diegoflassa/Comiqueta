---
description: "Measure the size of the AI context files and flag anything over budget."
disable-model-invocation: true
---

This is a pointer. The workflow is [`.agents/workflows/token-audit.md`](../../.agents/workflows/token-audit.md): read that file now and follow it exactly - its relative links resolve from its own folder. Any arguments typed after the command are its input.
