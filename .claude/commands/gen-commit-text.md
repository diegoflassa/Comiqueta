---
description: "Generate a conventional commit message for the current staged or working changes."
disable-model-invocation: true
---

This is a pointer. The workflow is [`.agents/workflows/gen-commit-text.md`](../../.agents/workflows/gen-commit-text.md): read that file now and follow it exactly - its relative links resolve from its own folder. Any arguments typed after the command are its input.
