---
description: "Sync the Knowledge Items with the current code after a change."
disable-model-invocation: true
---

This is a pointer. The workflow is [`.agents/workflows/update-kis.md`](../../.agents/workflows/update-kis.md): read that file now and follow it exactly - its relative links resolve from its own folder. Any arguments typed after the command are its input.
