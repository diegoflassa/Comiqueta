---
name: enum-vs-sealed
description: "A sealed hierarchy whose cases are all bare objects should be an enum. Use when declaring or reviewing any sealed class or sealed interface in domain or data code."
---

This is a pointer. The skill is [`.agents/skills/enum-vs-sealed/SKILL.md`](../../../.agents/skills/enum-vs-sealed/SKILL.md): read that file now and follow it exactly - its relative links resolve from its own folder.
